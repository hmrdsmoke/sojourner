/target
assets/web-source/
assets/web-usfm/
assets/web-html/
# The voice model (114 MB) and ONNX Runtime are obtained separately;
# assets/SOURCES.md has the URLs and hashes. The voice card and config
# beside the model are committed.
assets/voices/*.onnx
assets/onnxruntime/
